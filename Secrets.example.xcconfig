// Secrets.example.xcconfig — 模板。复制成 Secrets.xcconfig 并填入真实 token。
// Secrets.xcconfig 已被 .gitignore 忽略，绝不会进 repo。
//
//   cp Secrets.example.xcconfig Secrets.xcconfig
//   然后把下面的 REPLACE_ME 换成 jianshuo.dev/files 的 FILES_TOKEN
//
// token 来源见记忆 [[jianshuo-dev-files-transfer]]：它是 Pages secret，本体不落盘。

FILES_TOKEN = REPLACE_ME

// PostHog 项目 key（US cloud，app.posthog.com → Settings → Project）。
// 客户端公开 key，非机密，但统一走 xcconfig 注入；CI 由 build.yml 从
// GitHub secret POSTHOG_API_KEY 重建。留空 = App 内直接不启用 PostHog。
POSTHOG_API_KEY = REPLACE_ME
